# Runtime dependencies (native install without a build step).
# Preferred: `pip install .` which reads pyproject.toml.
pyserial>=3.5
paho-mqtt>=2.0
PyYAML>=6.0
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
